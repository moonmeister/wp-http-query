# Apache 2.4 + mod_proxy_fcgi -> php-fpm.
# The other half of the Apache question; mod_php is tested separately, since
# the two use different SAPIs and may not behave the same for unknown verbs.

ServerRoot "/usr/local/apache2"
Listen 80
ServerName localhost

LoadModule mpm_event_module   modules/mod_mpm_event.so
LoadModule authz_core_module  modules/mod_authz_core.so
LoadModule dir_module         modules/mod_dir.so
LoadModule mime_module        modules/mod_mime.so
LoadModule rewrite_module     modules/mod_rewrite.so
LoadModule proxy_module       modules/mod_proxy.so
LoadModule proxy_fcgi_module  modules/mod_proxy_fcgi.so
LoadModule unixd_module       modules/mod_unixd.so
LoadModule log_config_module  modules/mod_log_config.so

User daemon
Group daemon

DocumentRoot "/var/www/html"
DirectoryIndex index.php

ErrorLog /proc/self/fd/2
LogLevel warn
LogFormat "%h %l %u %t \"%r\" %>s %b" common
CustomLog /proc/self/fd/1 common

TypesConfig conf/mime.types

<Directory "/var/www/html">
    Options -Indexes +FollowSymLinks
    AllowOverride All
    Require all granted

    # Front-controller rewrite, as a WordPress install would have.
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /index.php [L]
</Directory>

<FilesMatch \.php$>
    SetHandler "proxy:fcgi://php-fpm:9000"
</FilesMatch>
